using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// This unfinished script was meant to let the game select which character the player would talk to
/// at the end of the initial group discussion. It would identify who had the highest personal 
/// values and randomly select between multiple of them if there was a tie.
/// Right now, it's just a few lines of commented-out pseudocode.
/// </summary>
public class CharacterFinalConvoSelector : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
        //PSEUDOCODE
		//1. Check all character variables, see who'se highest
        //2. If there is a tie, randomly choose
        //3. Based on above, choose one person to be the one, set all yarn vars accordingly
	}
}
