using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharacterFinalConvoSelector : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		//Check all character variables, see who'se highest
        //If there is a tie, randomly choose
        //Based on above, choose one person to be the one, set all yarn vars accordingly
	}
}
